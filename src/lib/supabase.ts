/**
 * Supabase client placeholder.
 * Wire up later by adding @supabase/supabase-js and the env vars below.
 * Use Lovable Cloud for zero-setup backend.
 *
 * Example future code:
 *
 *   import { createClient } from "@supabase/supabase-js";
 *   export const supabase = createClient(
 *     import.meta.env.VITE_SUPABASE_URL!,
 *     import.meta.env.VITE_SUPABASE_ANON_KEY!,
 *   );
 *
 * Suggested tables: pets, shelters, adopter_preferences, saved_pets,
 * adoption_applications, profiles.
 */

export const supabase = null;
